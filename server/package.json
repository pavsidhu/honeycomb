{
  "name": "honeycomb-server",
  "version": "1.0.0",
  "description": "TRPC server for Honeycomb",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/server.js"
  },
  "devDependencies": {
    "@types/node": "^18.11.3",
    "nodemon": "^2.0.20",
    "rimraf": "^3.0.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@trpc/server": "^10.0.0-proxy-beta.25",
    "knex": "^2.3.0",
    "objection": "^3.0.1",
    "pg": "^8.8.0",
    "zod": "^3.19.1"
  }
}
